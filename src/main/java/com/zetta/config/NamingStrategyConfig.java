package com.zetta.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for Hibernate naming strategy.
 * The naming strategy is configured via application.properties.
 */
@Configuration
public class NamingStrategyConfig {
    // Naming strategy is configured via application.properties
    // using spring.jpa.hibernate.naming.physical-strategy
}

